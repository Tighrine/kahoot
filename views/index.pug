extends layout

block variables
  - var text = `Thank you ${name} ! Now your accout is confirmed :)`
  - var title = "Kahoot App"

block content
  h2.text-center #{title}

  .row
    .offset-sm-2.col-sm-8
      .jumbotron.text-center.
            #{text}
